export enum UserRole {
    ADMIN = 'admin',
    CLIENT = 'client',
}

export enum LockerSize {
    SMALL = 'small',
    MEDIUM = 'medium',
    LARGE = 'large'
}

export enum LockerStatus {
    AVAILABLE = 'available',
    RESERVED = 'reserved',
    MAINTENANCE = 'maintenance'
}
